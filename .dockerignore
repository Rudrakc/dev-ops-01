# =============================================================================
# Docker Ignore File
# =============================================================================
# Only exclude files that are NOT needed for building the application
# Be conservative - better to include extra files than break the build
# =============================================================================

# Dependencies - reinstalled fresh in container
node_modules/

# Build output - rebuilt in container
dist/

# Test files - not needed in production image
tests/
coverage/
jest.config.js

# Git files
.git/
.gitignore

# IDE/Editor files
.vscode/
.idea/
*.swp
*.swo

# GitHub Actions - not needed in container
.github/

# Environment files - NEVER include secrets in images
.env
.env.local
.env.development
.env.production

# Kubernetes manifests - not needed in container
k8s/

# Docker files - not needed inside the image
Dockerfile
docker-compose*.yml
.dockerignore

# Documentation - not needed in container
README.md
docs/

# Logs
*.log
npm-debug.log*

# OS files
.DS_Store
Thumbs.db

# =============================================================================
# IMPORTANT: Do NOT ignore these files (needed for build):
# - package.json, package-lock.json (npm install)
# - tsconfig.json (TypeScript compilation)
# - src/ directory (source code)
# - .eslintrc.json (optional, but harmless to include)
# =============================================================================
